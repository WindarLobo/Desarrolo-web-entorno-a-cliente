// Abrir la consola para ver el resultado de la ejecución
for (let i = 0; i < 5; i++) {
  console.log("Bucle principal, valor " + i.toString());
  for (let j = 0; j < 3; j++) {
    console.log("Bucle anidado, valor " + j.toString());
  }
}
